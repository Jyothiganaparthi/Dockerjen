apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  selector:
     matchLabels:
      app: java
  type: LoadBalancer
  ports:
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007
    
